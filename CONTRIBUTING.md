# Contributing

Thanks for improving Starforge.

## Local setup

```bash
python -m pip install -e . pytest
python -m pytest
```

## Pull requests

Please include:

- The behavior change
- Tests for new checks or scoring changes
- Example CLI output when user-facing text changes

Keep checks objective and explainable. A failed check should produce a fix that a maintainer can act on immediately.
